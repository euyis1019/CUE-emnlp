% Appendix G: Cross-Task Analysis

\section{Cross-Task Analysis}
\label{app:cross_task_analysis}

% 本节提供 §4 中跨任务比较的完整数据，补充正文中因篇幅仅展示的关键对比。

\subsection{Outcome Fingerprints: Full Data}
\label{app:subsec:outcome_fingerprints_full}

% 六类任务的完整 four-way outcome 数值表，包含正文中省略的具体百分比。
% 预期效果：读者可查阅所有任务的完整 fingerprint 数据。

% TODO: 插入完整 outcome fingerprint 表格

\subsection{Computing Density Sweep}
\label{app:subsec:computing_density_sweep}

% Computing 任务中 $\rho$ 从 0 → 0.5 → 1 的单调效应数据与可视化。
% 展示 Resolved 下降、Overprocessed 上升、brewing duration 增长随 $\rho$ 的连续变化。
% 预期效果：支撑正文 "effects strengthen monotonically" 的 claim。

% TODO: 插入 rho sweep 数据与图表

\subsection{Per-Difficulty-Level Breakdown}
\label{app:subsec:per_difficulty_breakdown}

% 每类任务在各难度级别下的 outcome 分布细分。
% 预期效果：展示 outcome fingerprint 在难度增大时如何系统性变化。

% TODO: 插入 per-difficulty breakdown 表格/图

\subsection{Brewing Dynamics Comparison}
\label{app:subsec:brewing_dynamics_comparison}

% 六类任务的 $H_C^\ell$ descent curves、$A^\ell$ trajectories、$\Delta_{\mathrm{brew}}$ 跨任务对比。
% 预期效果：直观展示 data flow vs. control flow 任务的 brewing dynamics 差异。

% TODO: 插入 brewing dynamics 对比图
